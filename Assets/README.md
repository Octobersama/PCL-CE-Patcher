# 第三方组件说明 (Third-Party Component): CleanHost.exe

本目录包含 `CleanHost.exe`，它是 Microsoft .NET SDK 分发的原生 `apphost.exe` 二进制文件的副本（已重命名）。
This directory contains `CleanHost.exe`, which is a renamed copy of the native `apphost.exe` binary distributed with the Microsoft .NET SDK.

## 组件详情 (Component Details)
- **原始文件名 (Original Filename):** `apphost.exe`
- **来源 (Source):** Microsoft .NET 8.0 SDK (Windows x64)
- **用途 (Purpose):** 用作宿主模板，在重新打包单文件可执行程序时用于引导托管的 .NET 程序集。
- **Purpose:** Used as a template host application to bootstrap the managed .NET assembly when repacking the single-file executable.

## 许可信息 (Licensing Information)

**重要 (IMPORTANT):**

此文件 **不受** 本项目的 MIT 许可证覆盖。它是由 Microsoft Corporation 拥有的专有二进制文件。
This file is **NOT** covered by the project's MIT License. It is a proprietary binary owned by Microsoft Corporation.

此文件根据 **MICROSOFT 软件许可条款 (MICROSOFT .NET LIBRARY)** 进行分发。
This file is distributed under the **MICROSOFT SOFTWARE LICENSE TERMS (MICROSOFT .NET LIBRARY)**.

**[点击此处查看完整第三方声明 (Click here for full Third-Party Notices)](../ThirdPartyNotices.txt)**

根据许可条款第 3.a 节（“可分发代码”），此文件被归类为可分发代码，在遵守特定合规要求（本项目已遵守）的前提下，可以包含在基于该软件开发的程序中。